name: Inventory Comparison ETL

on:
  # Run on schedule (e.g., daily at 2 AM UTC)
  schedule:
    - cron: '0 2 * * *'
  
  # Allow manual trigger
  workflow_dispatch:
  
  # Optional: run on push to main (for testing)
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - 'data.py'
  #     - '.github/workflows/inventory-etl.yml'

jobs:
  run-etl:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Set up Google Cloud credentials
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
      
      - name: Run ETL script
        env:
          # Non-sensitive data from Repository Variables (vars)
          ODOO_URL: ${{ vars.ODOO_URL }}
          ODOO_DB: ${{ vars.ODOO_DB }}
          STORE_API_URL: ${{ vars.STORE_API_URL }}
          BIGQUERY_PROJECT: ${{ vars.BIGQUERY_PROJECT }}
          BIGQUERY_DATASET: ${{ vars.BIGQUERY_DATASET }}
          BIGQUERY_TABLE: ${{ vars.BIGQUERY_TABLE }}
          
          # Sensitive data from Repository Secrets (secrets)
          ODOO_USERNAME: ${{ secrets.ODOO_USERNAME }}
          ODOO_PASSWORD: ${{ secrets.ODOO_PASSWORD }}
          STORE_API_KEY: ${{ secrets.STORE_API_TOKEN }}
        run: |
          python data.py
      
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: etl-logs
          path: |
            *.log
            **/*.log
          retention-days: 7
      
      - name: Notify on failure
        if: failure()
        run: |
          echo "ETL job failed. Check the logs for details."
          # Optional: Add Slack/email notification here
